name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    # A newer commit supersedes an older BUILD still in flight — cancel it so the
    # newest content wins and rapid successive edits don't queue up (~5 min each).
    concurrency:
      group: pages-build-${{ github.ref }}
      cancel-in-progress: true
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11.5.0

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build with Astro
        run: pnpm run build

      # Headless Chrome to render each entry's downloadable PDF (with the
      # per-page footer) straight into dist/entries/<slug>.pdf, so the on-site
      # "Download (PDF)" button resolves. Same provisioning as zenodo-mint.yml.
      - name: Setup Chrome
        uses: browser-actions/setup-chrome@v2
        id: chrome
        with:
          chrome-version: stable

      # Persist rendered PDFs across deploys. The render script (pnpm pdfs:site)
      # re-renders only entries whose built index.html hash changed and copies
      # the rest from this cache, so a content edit re-renders one PDF and an
      # unrelated change re-renders none. restore-keys pulls the most recent
      # cache; a new one is saved per commit SHA.
      - name: Cache rendered entry PDFs
        uses: actions/cache@v6
        with:
          path: .pdf-cache
          key: pdf-cache-v2-${{ github.sha }}
          restore-keys: |
            pdf-cache-v2-

      - name: Render entry PDFs into dist/entries
        env:
          CHROME_BIN: ${{ steps.chrome.outputs.chrome-path }}
        run: pnpm run pdfs:site

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    # Serialize the actual publish and NEVER cancel one mid-flight, so a live
    # deployment is never left half-published.
    concurrency:
      group: pages-deploy
      cancel-in-progress: false
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
